# 《熄灯前归寝》软件工程实践说明

**课程模块：** 软件架构设计、小组软件过程与项目管理、软件分支管理、软件代码评审、持续集成与部署  
**项目名称：** 《熄灯前归寝》（World of Zuul 扩展）  
**小组：** 肖梦琪（组长）、刘晶、彭慧星、庞绮君  
**周期：** 2026 年 6 月 1 日 — 6 月 20 日  
**仓库：** https://github.com/wutcst/kai-fa-519club  

本文档对应课程五项实践要求，结合本组选题说明**采用的架构、过程、分支、评审与 CI/CD 做法**，并说明与 Spring / Vue 等课程技术点的对应关系。

---

## 一、软件架构设计

### 1.1 主要内容回应

本选题是**闯关解密类桌面游戏**（文本模式 + JavaFX/Swing GUI + 可选 Socket 联机 + H2 存档），并非典型 B/S 电商系统，因此采用**分层架构 + 命令模式**，在合适层次上**对照** Spring Boot / Spring MVC / RESTful / Vue 的设计思想，而非强行将全部业务迁入 Web 栈。

### 1.2 总体架构（四层）

```
┌─────────────────────────────────────────────────────────┐
│  表现层（Presentation）                                   │
│  Parser / JavaFX·Swing GUI / 联机客户端输出               │
├─────────────────────────────────────────────────────────┤
│  应用层（Application）                                    │
│  Game、LevelManager、LevelTimer、CommandWords、Parser     │
├─────────────────────────────────────────────────────────┤
│  领域层（Domain）                                         │
│  Room、Player、Item、TeleportRoom、NPC、LevelConfig       │
├─────────────────────────────────────────────────────────┤
│  基础设施层（Infrastructure）                             │
│  H2 存档、Socket 多人服务端、Maven 构建、GitHub Actions   │
└─────────────────────────────────────────────────────────┘
```

**表现层**负责接收玩家输入、展示房间描述、背包与「距熄灯（23:00）还有 XXX 秒」。文本模式下 `Parser` 读入命令；图形界面（F7）将按钮/输入框映射为同一套命令字符串，实现**界面与逻辑分离**，对应 Spring MVC 中 **View** 的职责。

**应用层**中的 `Game` 协调关卡流程；`LevelManager` 管理五关解锁与失败重开；`LevelTimer` 维护熄灯倒计时；各 `XxxCommand` 继承抽象类 `Command`，由 `CommandWords` 注册分发。该结构对应 Spring MVC 的 **Controller + 路由**：命令名即“路径”，`execute(Game)` 即“处理请求”，**不**把业务规则写在 `Game.play()` 的巨型分支里。

**领域层**封装房间拓扑、物品、玩家负重、条件门、黑暗区等规则，对应 Spring 中的 **Service / Domain Model**，与界面、数据库解耦。

**基础设施层**实现 H2 持久化（F8）、多人 Socket 或可选 REST 网关（F6）、CI 打包发布，对应 **Repository / 外部集成**。

### 1.3 与 Spring / Spring Boot / Spring MVC / RESTful / Vue 的对照

| 课程技术 | 本项目实践方式 | 说明 |
|----------|----------------|------|
| **Spring MVC** | 命令模式 + `Game` 调度 | `Command` ≈ Controller；`Room`/`Player` ≈ Model；GUI/控制台 ≈ View |
| **Spring Boot** | Maven 可执行 JAR + 可选联机服务端模块 | 主程序 `Main` 一键启动；联机服务端可独立 `main`，内嵌 Tomcat 非必须 |
| **RESTful API** | F6 联机协议（推荐 JSON over HTTP 或 Socket 文本协议） | 资源示例：`POST /api/game/command`、`GET /api/game/state`；若首版用 Socket，文档中仍按 REST 资源风格设计消息体，便于扩展 Web 大厅 |
| **Vue** | F7 桌面 GUI 为主；**可选** Vue 管理页 | 排行榜/存档查询可用 Vue + REST 只读展示；游戏内操作仍以 JavaFX/Swing 实时交互 |

**架构选型结论：** 对本选题而言，**桌面分层 + 命令模式**是“适当架构”；Spring Boot 适用于**联机大厅、存档 API、统计后台**等可拆子系统；Vue 适用于**运维/数据展示页**。核心玩法（五关、计时、物品）保持在 Java 领域层，避免前后端过度拆分导致联调成本过高。

### 1.4 关键设计决策（段落说明）

房间连接关系在 `Game.createRooms()` 中**一次定义、全局不变**，关卡差异通过 `LevelManager` 解锁出口与刷新物品实现，避免每关重建地图。拓展命令 `use`、`submit`、`combine` 等独立于已实现之 F5 `take`/`drop`，符合开闭原则。熄灯倒计时 `LevelTimer` 为横切关注点，由应用层统一扣减，表现层只读显示秒数。H2 仅持久化关卡号、剩余秒数、通关记录，不序列化整个对象图，降低基础设施层复杂度。

### 1.5 实践重点自检（架构）

- [x] 划分表现 / 应用 / 领域 / 基础设施层  
- [x] 命令模式对应 MVC 中控制器职责  
- [x] GUI 与文本共用同一套 `Command` 执行链  
- [ ] F6 联机接口按 RESTful 或类 REST 消息设计（开发中）  
- [ ] F7 图形界面绑定应用层状态（开发中）  
- [ ] 可选：Spring Boot 子模块提供存档/排行榜 API（视进度选做）  

---

## 二、小组软件过程与项目管理

### 2.1 主要内容回应

本组基于 **DevOps 思想**，使用 **GitHub** 作为统一项目管理平台，覆盖需求（Issue）、文档（`docs/`）、代码（仓库）、自动化流水线（Actions）与发布（`master` + JAR 制品）。不单独搭建 Jira，以降低课程项目运维成本，但**过程活动与 DevOps 闭环一致**：计划 → 分支开发 → PR → 评审 → CI → 合并 → 发布。

### 2.2 软件过程阶段划分

| 阶段 | 时间 | 目标 | 主要产出 |
|------|------|------|----------|
| **立项与策划** | 6/1 — 6/3 | 定选题、五关流程、功能 F/E 编号 | [会议记录.md](会议记录.md) |
| **迭代一：框架** | 6/1 — 6/7 | E9/E10/E11、拓展命令雏形、第 1—2 关 | `feature/level-manager`、`feature/use-submit` |
| **迭代二：功能** | 6/8 — 6/14 | 五关贯通、E 系列补齐 | 各 `feature/level-*` |
| **迭代三：集成** | 6/15 — 6/18 | F6/F7/F8 联调、冻结功能 | `feature/multiplayer-server`、`feature/gui` |
| **发布与收尾** | 6/19 — 6/20 | 测试、报告、视频、`master` 发布 | REPORT、演示视频、Artifacts |

过程模型为**短周期迭代 + 主干集成（dev）**，每迭代结束要求 `dev` 可构建、可演示，符合课程“开发阶段划分”要求。

### 2.3 项目管理平台与工具

| 用途 | 工具 | 本组用法 |
|------|------|----------|
| 代码仓库 | GitHub | `wutcst/kai-fa-519club` |
| 任务与缺陷 | GitHub Issues | 按 E1—E17、F6—F8 建 Issue，PR 描述关联 |
| 文档 | `docs/`、`README.md` | 会议记录、本实践说明、分工与功能清单 |
| 协作规范 | `CONTRIBUTING.md` | 分支命名、提交格式、门禁命令 |
| 代码评审 | GitHub Pull Request | 模板见 `.github/PULL_REQUEST_TEMPLATE.md` |
| 持续集成 | GitHub Actions | `.github/workflows/maven.yml` |

### 2.4 开发任务分派与跟踪

任务按**功能编号 + 成员权重**分派（肖梦琪约 32%、刘晶约 30%、彭慧星约 19%、庞绮君约 19%），详见 [会议记录.md](会议记录.md)。跟踪方式：Issue 指派负责人 → 对应 `feature/xxx` 分支 → PR 合并后关闭 Issue；组长每周在例会同步 `dev` 与 `master` 差异及阻塞项。README 中功能勾选表（F/E 列表）作为**进度看板**，全组可见完成度。

### 2.5 实践重点自检（过程与项目管理）

- [x] 划分立项 / 迭代 / 集成 / 发布阶段  
- [x] GitHub 作为统一管理平台  
- [x] 会议记录、分工权重、时间表文档化  
- [x] Issue — 分支 — PR — 合并 可追溯  
- [ ] 全部 E/F 功能 Issue 创建并关闭（进行中）  

---

## 三、软件分支管理

### 3.1 主要内容回应

本组采用 **Git Flow 简化版**：长期存在 **`master`（发布）** 与 **`dev`（集成）**；个人开发在 **`feature/xxx`**；缺陷在 **`fix/xxx`**。不在仓库长期保留 `release/*` 分支，课程演示版本以 **`master` 标签 + Actions 制品** 为准；若发布前需冻结，由组长从 `dev` 打 tag（如 `v1.0.0`）。

### 3.2 分支模型说明

| 分支类型 | 命名 | 用途 | 谁可合并 |
|----------|------|------|----------|
| **主分支** | `master` | 答辩与对外发布的稳定版本 | 仅组长 PR：`dev` → `master` |
| **开发分支** | `dev` | 日常集成，所有功能合并目标 | 组员 PR，组长 Review |
| **功能分支** | `feature/xxx` | 个人功能开发，如 `feature/use-submit` | 开发者 PR → `dev` |
| **修复分支** | `fix/xxx` | Bug 修复，如 `fix/timer-display` | 开发者 PR → `dev` |

**debug 分支：** 课程所称 debug 分支，本组以 **`fix/xxx`** 实现相同目的（专用于缺陷定位与修复）；若需长时间实验性调试，可临时使用 `fix/debug-简述`，合并前须通过 CI，禁止直接向 `master` 推送。

### 3.3 标准工作流（段落）

组员每日开发前执行 `git checkout dev` 与 `git pull origin dev`，从最新 `dev` 创建 `feature/xxx`。功能完成且本地 `mvn checkstyle:check`、`mvn test` 通过后推送远程并发起 PR 至 `dev`。组长或同伴 Review 通过且 CI 绿灯后合并；合并后开发者删除本地功能分支并再次 `pull dev` 同步。发布周由组长发起 `dev` → `master` 的 PR，通过后在 Actions 下载 `game-jar` 作为答辩制品。

### 3.4 代码冲突与解决

冲突多发生于 `Game.java`、`CommandWords.java`、`pom.xml` 等集成热点。预防策略：**按模块分文件**（命令一人一组、关卡配置独立类）、小步提交、每日合并 `dev`。出现冲突时，在本地 `git pull origin dev` 或 `git merge dev` 后由改动人手动解决，**禁止**不经 Review 强制推送。解决后必须重新运行 `mvn test` 与 `mvn checkstyle:check`，PR 中注明冲突文件与验证结果。

### 3.5 实践重点自检（分支管理）

- [x] 主分支 `master`、开发分支 `dev` 职责清晰  
- [x] 功能分支 `feature/xxx`、修复分支 `fix/xxx` 已文档化  
- [x] 合并流程与冲突处理写入 `CONTRIBUTING.md`  
- [ ] 发布 tag `v1.0.0` 打在 `master`（发布周完成）  

---

## 四、软件代码评审

### 4.1 主要内容回应

本组将代码评审作为 **合并 `dev` 的必经环节**，以 **GitHub Pull Request 同行评审（Peer Review）** 为主，关键模块（计时器、关卡切换、联机同步）由组长与另一组员**结对过读（Pair Review）**，不替代个人自测与 CI。

### 4.2 评审类型与适用场景

| 类型 | 本组做法 | 适用 |
|------|----------|------|
| **同行评审（Code Review）** | 每个 PR 至少 1 人 Approve（组长或模块负责人） | 全部 `feature/*`、`fix/*` → `dev` |
| **结对编程 / 结对评审** | 肖梦琪 + 刘晶联调命令与 `LevelManager`；彭慧星 + 肖梦琪对接 F6 协议 | 架构热点、接口变更 |
| **自检清单** | PR 模板：Checkstyle、JUnit、手动场景、是否影响熄灯计时 | 提交者填写 |

### 4.3 评审关注点

评审不仅看格式，更看：**是否破坏 F1—F5 已有行为**；拓展命令是否在 `CommandWords` 正确注册；`LevelTimer` 扣秒是否与 GUI 一致；H2 读写是否覆盖异常；多人命令是否破坏服务端权威状态。评论需具体（文件 + 行 + 建议），作者修改后回复或 push 新 commit，直至 Approve。

### 4.4 工具与记录

- **工具：** GitHub PR「Files changed」、行内评论、Review summary。  
- **规范：** [CONTRIBUTING.md](../CONTRIBUTING.md) 提交信息须写清 `feat:` / `fix:` 描述。  
- **模板：** [.github/PULL_REQUEST_TEMPLATE.md](../.github/PULL_REQUEST_TEMPLATE.md) 强制自查项。  

### 4.5 实践重点自检（代码评审）

- [x] PR 模板与合并前 Review 流程已建立  
- [x] 组长负责 `dev` → `master` 终审  
- [ ] 每个已合并 PR 留有至少 1 条 Review 记录（迭代中积累）  

---

## 五、持续集成与部署

### 5.1 主要内容回应

本组在 **GitHub Actions** 上实现 **持续集成（CI）**：推送 `dev`、`feature/**`、`fix/**` 或向 `master`/`dev` 提 PR 时自动执行规范检查、单元测试与打包，产出可执行 JAR。**持续部署（CD）** 限于课程场景：制品上传至 Actions Artifacts，答辩现场下载运行；不强制上云容器生产部署，但保留 Docker 扩展说明。

### 5.2 CI 流水线（`.github/workflows/maven.yml`）

流水线名称 **CI Pipeline**，主要步骤如下：

1. **Checkout** 拉取代码  
2. **Set up JDK 11**（Temurin，Maven 缓存）  
3. **Checkstyle** — `mvn checkstyle:check`（validate 阶段规范门禁）  
4. **单元测试** — `mvn test`（JUnit 5，Surefire 报告上传）  
5. **构建打包** — `mvn package -DskipTests=false`  
6. **上传制品** — `*-jar-with-dependencies.jar` 至 Artifact `game-jar`  
7. **上传测试报告** — `target/surefire-reports/`  

任一 step 失败则 PR **不可合并**（建议开启分支保护规则）。

### 5.3 自动化测试策略

| 层次 | 工具 | 范围 |
|------|------|------|
| 静态规范 | Checkstyle | 全源码 + 测试代码 |
| 单元测试 | JUnit 5 | `RoomTest`、命令类、计时与关卡逻辑（持续补充） |
| 集成/手动 | 组内试玩清单 | 五关通关、熄灯失败、submit 错物、读档 |

本地门禁与 CI 一致：`mvn checkstyle:check` → `mvn test` → `mvn package`。

### 5.4 打包与发布

- **构建命令：** `mvn clean package`  
- **运行：** `java -jar target/world-of-zuul-1.0.0-SNAPSHOT-jar-with-dependencies.jar`  
- **发布：** 组长将 `dev` 合并至 `master` 后，从 Actions 下载 `game-jar` 或本地 `mvn package` 产物，附于实训报告与 B 站演示。  
- **版本标识：** 建议在 `master` 打 Git tag `v1.0.0` 与报告版本一致。  

### 5.5 容器管理与运行（课程扩展说明）

当前首版以 **JAR 直跑** 为主。若联机服务端需隔离环境，可选用 **Docker** 多阶段构建：阶段一 `maven:3.8-eclipse-temurin-11` 编译，阶段二 `eclipse-temurin:11-jre` 仅包含 JAR 与 H2 数据卷挂载路径。容器非本阶段必交付项，写入报告体现对「容器管理与运行」的理解即可。

示例 `Dockerfile` 思路（伪代码，按需添加至仓库）：

```dockerfile
FROM maven:3.8-eclipse-temurin-11 AS build
WORKDIR /app
COPY . .
RUN mvn -q package -DskipTests=false

FROM eclipse-temurin:11-jre
WORKDIR /app
COPY --from=build /app/target/*-jar-with-dependencies.jar app.jar
ENTRYPOINT ["java", "-jar", "app.jar"]
```

### 5.6 实践重点自检（CI/CD）

- [x] 自动化 Checkstyle + JUnit + 打包脚本（GitHub Actions）  
- [x] 测试报告与 JAR 制品上传  
- [x] 本地与 CI 命令统一（Maven）  
- [ ] 分支保护：CI 通过才可合并 `dev`（建议组长在 GitHub 设置）  
- [ ] 发布 tag 与答辩 JAR 归档（6/20 前）  

---

## 六、五项实践总览表

| 课程模块 | 主要内容 | 本组实践重点 | 文档/配置位置 |
|----------|----------|--------------|----------------|
| 软件架构设计 | 适当架构、开发框架 | 四层架构 + 命令模式；对照 Spring MVC / 可选 REST、Vue | 本文 §一、[会议记录.md](会议记录.md) |
| 小组过程与项目管理 | DevOps、任务分派跟踪 | GitHub Issues + 四阶段迭代 + 分工权重 | 本文 §二、[README.md](../README.md) |
| 软件分支管理 | 主/发布/开发/debug 分支 | `master` / `dev` / `feature/*` / `fix/*` | 本文 §三、[CONTRIBUTING.md](../CONTRIBUTING.md) |
| 软件代码评审 | 同行评审、结对、Review | PR 模板 + Approve 合并 | 本文 §四、PR 模板 |
| 持续集成与部署 | 自动测试、CI、打包发布 | GitHub Actions Maven 流水线 + JAR 制品 | 本文 §五、`maven.yml` |

---

## 七、参考文献与仓库索引

- 项目说明：[README.md](../README.md)  
- 贡献与分支：[CONTRIBUTING.md](../CONTRIBUTING.md)  
- 会议纪要：[会议记录.md](会议记录.md)  
- CI 配置：[.github/workflows/maven.yml](../.github/workflows/maven.yml)  

---

*本文档供实训报告「软件工程过程」「架构设计」「DevOps 实践」等章节直接引用或节选；与代码实现进度同步更新。*
